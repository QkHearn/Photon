# Skill 动态激活机制 - 总结

> 问题: "配置文件允许了 Skill,但什么时候才真正加载/注入 Prompt 里?"
> 答案: **运行时按需激活,Just-In-Time 注入**

---

## 核心答案

### 三层分离架构

```
┌─────────────────────────────────────────────────────────────┐
│ 1. 配置层 (config.json)                                      │
│    作用: 定义 Skill Allowlist                                │
│    时机: 启动前                                               │
│    内容: agent.skillRoots = ["~/.photon/skills"]            │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 2. 加载层 (Startup)                                          │
│    作用: 加载 Skill 元数据,不注入内容                        │
│    时机: Agent 启动时                                         │
│    操作: SkillManager.loadInternalStaticSkills()            │
│    内容: {name, description, tools, constraints}            │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 3. 发现层 (Initial Prompt)                                   │
│    作用: 告知 LLM 有哪些 Skill 可用                          │
│    时机: 组装 System Prompt 时                               │
│    内容: Skill 名称 + 简短描述 (仅列表)                      │
│    方法: getSkillDiscoveryPrompt()                          │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 4. 激活层 (Runtime - LLM 决策)                               │
│    作用: LLM 决定需要某个 Skill                              │
│    时机: Plan Phase                                          │
│    操作: LLM 调用 skill_activate("skill_name")              │
│    验证: SkillManager 检查 Allowlist                         │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 5. 注入层 (Dynamic Prompt Injection)                         │
│    作用: 注入 Skill 的工具、约束、接口                       │
│    时机: Skill 激活后,下一次 Plan Phase 前                  │
│    内容: Allowed Tools + Constraints + Interface            │
│    方法: getActiveSkillsPrompt()                            │
└─────────────────────────────────────────────────────────────┘
```

---

## 关键时间点

| 阶段 | Skill 状态 | Prompt 内容 | 时机 |
|------|-----------|-------------|------|
| **配置** | 定义 Allowlist | 无 | 启动前 |
| **启动** | 加载元数据 | 发现列表 (仅名称) | Agent 初始化 |
| **运行** | 未激活 | 发现列表 | Plan Phase (首次) |
| **激活** | 激活中 | **完整 Prompt** | tool_call: skill_activate |
| **使用** | 激活完成 | **完整 Prompt** | Plan Phase (后续) |
| **停用** | 已停用 | 移除 Prompt | tool_call: skill_deactivate |

---

## 完整流程示例

```
[时间点 T0] Agent 启动
├─ config.json 定义 Allowlist: ["project_scan", "code_modify"]
├─ SkillManager 加载元数据
└─ System Prompt 注入:
   """
   Available Skills (NOT active):
   - project_scan: Scan project files
   - code_modify: Modify code files
   
   ⚠️  Skills must be activated via skill_activate(name)
   """

[时间点 T1] 用户请求
用户: "扫描项目中所有 Python 文件"

[时间点 T2] Iteration 1 - Plan Phase
LLM 输入:
  - System Prompt (包含 Skill 发现列表)
  - User Message: "扫描项目..."
  
LLM 分析:
  - 需要扫描文件
  - 检查可用 Skill: project_scan 可用
  - 决策: 先激活 Skill
  
LLM 输出:
  - tool_call: skill_activate(skill_name="project_scan")

[时间点 T3] Act Phase
执行 skill_activate("project_scan"):
  1. SkillManager.activate("project_scan")
  2. 检查 Allowlist ✓
  3. 标记为激活
  4. 返回成功 + Skill Prompt

[时间点 T4] Iteration 2 - Plan Phase (动态注入)
LLM 输入:
  - System Prompt (原有内容)
  - **新注入**: 
    """
    # ACTIVATED SKILLS
    
    ## Skill: project_scan
    Allowed Tools:
      - list_project_files
      - read_code_block
    Constraints:
      - Read-only access
      - Max 500 lines per read
    """
  - Previous tool results
  
LLM 分析:
  - 现在可以看到 project_scan 的工具
  - 可以使用 list_project_files
  
LLM 输出:
  - tool_call: list_project_files(pattern="**/*.py")

[时间点 T5] Act Phase
执行 list_project_files:
  - 返回文件列表

[时间点 T6] Iteration 3 - Plan Phase
LLM 使用 Skill 的工具完成任务...

[时间点 T7] 任务完成 (可选清理)
LLM 输出:
  - tool_call: skill_deactivate(skill_name="project_scan")
  
执行 skill_deactivate:
  - SkillManager.deactivate("project_scan")
  - 移除 Skill Prompt (下次 Plan 时不再注入)
```

---

## 关键设计决策

### 1. 为什么不在启动时注入所有 Skill?

**问题**:
- Token 浪费: 10 个 Skill × 500 tokens = 5K tokens
- 每次对话都付费,但大部分 Skill 不会被用到

**解决方案**:
- 启动时只注入"发现列表" (10 × 50 tokens = 500 tokens)
- 激活时才注入完整内容 (仅激活的 Skill)
- 节省: 90% Token 成本

### 2. 为什么需要显式激活?

**问题**:
- 如何让 LLM 知道何时可以使用 Skill?
- 如何控制 Prompt 注入时机?

**解决方案**:
- 提供 `skill_activate` 工具
- LLM 自主决定何时激活
- Agent 在工具执行后动态注入 Prompt

### 3. 为什么不在 SKILL.md 中嵌入所有逻辑?

**问题**:
- SKILL.md 可能很长 (数千行)
- 全部注入会浪费 Token

**解决方案**:
- 只注入"最小接口" (Allowed Tools + Constraints)
- 完整文档仅在需要时通过 `skill_read` 获取
- 典型 Skill Prompt: 200-500 tokens

---

## 工业级特性

### ✅ Token 效率

```
场景: 20 个可用 Skill,平均使用 2 个

全量注入:
  - 每轮对话: 20 × 500 = 10K tokens
  - 100 轮: 1M tokens

动态激活:
  - 发现 Prompt: 20 × 50 = 1K tokens
  - 激活 Prompt: 2 × 500 = 1K tokens
  - 总计每轮: 2K tokens
  - 100 轮: 200K tokens

节省: 80% (800K tokens = $8.00)
```

### ✅ 可扩展性

```
企业级系统: 200 个 Skill

全量注入:
  - Prompt 大小: 200 × 500 = 100K tokens
  - 问题: 超过大部分模型上下文限制
  - 结论: 不可行

动态激活:
  - 发现 Prompt: 200 × 50 = 10K tokens
  - 激活 Prompt: 3 × 500 = 1.5K tokens (平均激活 3 个)
  - 总计: 11.5K tokens
  - 结论: 完全可行
```

### ✅ 安全可控

```
配置层: Allowlist 强制
  - config.json 定义允许的 Skill
  - 启动时加载验证
  
激活层: Runtime 检查
  - 每次激活验证 Allowlist
  - 激活失败不影响系统
  
审计层: 可观测
  - 记录所有激活事件
  - 统计 Skill 使用频率
```

### ✅ 智能决策

```
Agent 自主判断:
  - 任务分析 → 识别需要的 Skill
  - 按需激活 → 只激活必要的 Skill
  - 阶段切换 → 停用不再需要的 Skill

示例:
  任务: "实现功能 + 编写测试 + 更新文档"
  
  阶段 1: 激活 feature_development
  阶段 2: 停用 feature_development,激活 test_generation
  阶段 3: 停用 test_generation,激活 documentation
  
  优势: 每个阶段保持最小 Prompt 大小
```

---

## 实现状态

### ✅ 已完成

1. **架构设计**
   - 三层分离 (配置/加载/激活)
   - Skill 生命周期定义
   - Prompt 注入策略

2. **核心 API**
   - `SkillManager.activate(name)`
   - `SkillManager.deactivate(name)`
   - `SkillManager.getSkillDiscoveryPrompt()`
   - `SkillManager.getActiveSkillsPrompt()`

3. **工具实现**
   - `SkillActivateTool`
   - `SkillDeactivateTool`

4. **AgentRuntime 集成**
   - Skill 管理接口
   - Prompt 注入框架

5. **文档**
   - 设计文档 (SKILL_ACTIVATION_DESIGN.md)
   - 使用示例 (SKILL_ACTIVATION_EXAMPLE.md)
   - 实现清单 (SKILL_ACTIVATION_TODO.md)
   - Constitution 更新

### 🚧 待集成

1. **元数据解析**
   - 从 SKILL.md frontmatter 解析 tools, constraints

2. **Prompt 注入**
   - 取消 AgentRuntime 中的 TODO 注释
   - 启用动态注入逻辑

3. **工具注册**
   - 在 main.cpp 中注册 SkillActivateTool
   - 传递 SkillManager 到 AgentRuntime

4. **测试验证**
   - 创建测试 Skill
   - 端到端测试
   - 性能基准测试

---

## 核心价值

### 1. 解决的问题

❌ **旧方式**:
```
启动时将所有 Skill 注入 Prompt
→ Token 浪费
→ 上下文污染
→ 不可扩展
```

✅ **新方式**:
```
运行时按需激活 Skill
→ Token 高效
→ 上下文清晰
→ 无限扩展
```

### 2. 适用场景

✅ **完美适配**:
- 企业级 Agent 系统 (数十到数百个 Skill)
- 长时间运行任务 (数百轮对话)
- 成本敏感应用 (需要优化 Token 使用)

⚠️ **可能过度**:
- 玩具项目 (< 5 个 Skill)
- 一次性任务 (< 10 轮对话)

### 3. 与其他系统对比

| 系统 | Skill 加载方式 | Token 效率 | 可扩展性 |
|------|---------------|-----------|----------|
| **Photon (动态)** | 按需激活 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| AutoGPT | 全量注入 | ⭐⭐ | ⭐⭐ |
| LangChain | 全量注入 | ⭐⭐ | ⭐⭐⭐ |
| Microsoft Semantic Kernel | 按需加载 | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ |

---

## 总结

### 核心回答

**问题**: 配置文件允许了 Skill,但什么时候才真正加载/注入 Prompt 里?

**答案**:
1. **配置文件 = Allowlist**: 只定义允许哪些 Skill,不触发加载
2. **启动时 = 元数据加载**: 加载 Skill 信息,但不注入 Prompt
3. **初始 Prompt = 发现列表**: 注入 Skill 名称列表,告知 LLM 有哪些可用
4. **运行时 = 按需激活**: LLM 决定需要时,调用 `skill_activate`
5. **激活后 = 动态注入**: Agent 将 Skill 的工具、约束注入到 Prompt
6. **停用后 = 移除 Prompt**: (可选) 释放不再需要的 Skill

### 关键原则

```
配置 ≠ 加载 ≠ 激活 ≠ 注入

配置: 定义边界
加载: 准备数据
激活: 运行时决策
注入: Just-In-Time
```

### 最佳实践

1. **Allowlist 在配置层强制**: 安全第一
2. **元数据启动时加载**: 准备就绪
3. **发现列表初始注入**: 告知可能性
4. **激活由 LLM 决定**: 智能判断
5. **Prompt 动态注入**: 按需提供
6. **停用优化成本**: (可选) 精细控制

---

这是工业级 Agent 系统的标准做法,确保:
- ✅ Token 效率最大化
- ✅ 可扩展到数百个 Skill
- ✅ 安全可控可审计
- ✅ Agent 智能决策
- ✅ 上下文清晰不污染

**这就是 Photon Skill 动态激活机制的完整答案。**
