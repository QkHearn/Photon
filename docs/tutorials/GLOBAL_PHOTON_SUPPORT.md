# 🌍 全局 Photon 支持指南

## 📍 核心能力

Photon 现在支持作为**全局命令行工具**使用，可在任意目录下分析任意项目：

```bash
# 场景 1: 分析当前目录
photon .

# 场景 2: 分析指定项目
photon /path/to/your/project

# 场景 3: 分析其他位置的文件
photon /Users/hearn/other-project
```

## 🎯 路径处理增强

### ✅ 支持的路径格式

```bash
# 相对路径
> read_code_block("src/main.cpp")
> read_code_block("./src/main.cpp") 
> read_code_block("../other/file.cpp")

# 绝对路径
> read_code_block("/Users/hearn/project/src/main.cpp")
> read_code_block("/tmp/test.cpp")
```

### 🔧 智能路径处理

**核心逻辑**: 自动识别路径类型，智能处理

```cpp
fs::path inputPath = fs::u8path(filePath);
fs::path fullPath;

if (inputPath.is_absolute()) {
    // 绝对路径: 直接使用
    fullPath = inputPath;
} else {
    // 相对路径: 相对于项目根目录
    fullPath = rootPath / inputPath;
}
```

## 🚀 使用场景示例

### 场景 1: 项目内文件分析
```bash
cd /Users/hearn/my-project
photon .

> read_code_block("src/main.cpp")           # ✅ 有符号摘要
> read_code_block("./src/main.cpp")         # ✅ 有符号摘要  
> read_code_block("/full/path/to/file.cpp") # ✅ 有符号摘要
```

### 场景 2: 跨项目文件分析
```bash
cd /Users/hearn/project-a
photon .

# 分析其他项目文件
> read_code_block("/Users/hearn/project-b/src/main.cpp")  # ✅ 可读取内容
> read_code_block("../project-b/src/main.cpp")            # ✅ 可读取内容
```

**结果**: 
- ✅ 文件内容正常读取
- ⚠️ 无符号摘要（不在当前项目索引中）

## 📊 功能对比

| 文件位置 | 路径类型 | 内容读取 | 符号摘要 | 性能 |
|---------|----------|----------|----------|------|
| 项目内 | 相对/绝对 | ✅ | ✅ | 快速 |
| 项目外 | 绝对路径 | ✅ | ⚠️ | 正常 |
| 项目外 | 相对路径 | ✅ | ⚠️ | 正常 |

## 💡 核心优势

### 1. **零配置使用**
- 添加到 PATH 即可全局使用
- 自动识别项目根目录
- 智能路径解析

### 2. **灵活的代码分析**
- 支持任意路径格式
- 项目内文件享受符号索引
- 项目外文件降级到全文读取

### 3. **渐进式能力**
- 项目内 → 完整的符号分析
- 项目外 → 基础的内容读取
- 自动适配，无需手动配置

## 🎉 快速开始

```bash
# 1. 添加到环境变量
export PATH=$PATH:/path/to/photon

# 2. 在任意项目目录使用
photon .

# 3. 分析代码
> read_code_block("src/main.cpp")           # 项目内文件
> read_code_block("/other/path/file.cpp")   # 项目外文件
```

**现在，Photon 真正做到了 "随时随地，想分析就分析！"** 🎯
EOF 2>&1
